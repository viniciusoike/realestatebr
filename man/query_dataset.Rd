% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_dataset.R
\name{query_dataset}
\alias{query_dataset}
\title{Query a Large Dataset Lazily}
\usage{
query_dataset(name, table = NULL, version = "latest", quiet = FALSE)
}
\arguments{
\item{name}{Character. Dataset identifier. See \code{\link[=list_datasets]{list_datasets()}} for
datasets whose \code{access_mode} is \code{"query"}.}

\item{table}{Character or \code{NULL}. Return one table when supplied. When
\code{NULL}, return a named catalog containing every related table.}

\item{version}{Character. Immutable dataset version, or \code{"latest"} to use
the version referenced by the latest manifest.}

\item{quiet}{Logical. If \code{TRUE}, suppress informational messages.}
}
\value{
If \code{table} is supplied, a closable lazy \code{dbplyr} table. Otherwise,
a \code{realestatebr_query_dataset} catalog containing named lazy tables. Close
either result explicitly with \code{close()} when it is no longer needed.
}
\description{
Opens a large dataset as one or more lazy DuckDB tables. No observations
enter R memory until the query is explicitly collected with
\code{\link[dplyr:compute]{dplyr::collect()}}. Related tables returned in the same catalog share one
database connection and can be joined with standard dplyr verbs.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
cno <- query_dataset("cno")
on.exit(close(cno))

constructions_sp <- cno$constructions |>
  dplyr::filter(.data$state == "SP")

result <- constructions_sp |>
  dplyr::inner_join(cno$areas, by = "cno") |>
  dplyr::collect()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_dataset]{get_dataset()}} for datasets returned directly in memory.
}
