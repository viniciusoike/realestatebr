# Load Dataset Registry from YAML

Internal function to load the dataset registry from the
inst/extdata/datasets.yaml file.

## Usage

``` r
load_dataset_registry()
```

## Value

A list containing the parsed YAML registry
