# Show Dataset Import Message

Display informative message about which table was imported and what's
available

## Usage

``` r
show_import_message(name, table_info)
```

## Arguments

- name:

  Dataset name

- table_info:

  Result from validate_and_resolve_table()
