# Show Dataset Import Message

Reports the dataset, the table served, and the source that answered, in
a single message. Datasets resolved to their default table also list the
other tables available.

## Usage

``` r
show_import_message(name, table_info, tier)
```

## Arguments

- tier:

  Character. One of "memo", "github", or "fresh".
