# Conditional warning based on warning level

Conditional warning based on warning level

## Usage

``` r
warn_if_level(message, level = "user", warn_level = "user")
```

## Arguments

- message:

  Warning message

- level:

  Warning level: "user" (default), "dev" (development only)

- warn_level:

  Current warning level setting
