# Extract Specific Table from Property Records Data

Internal helper function to extract a specific table from cached
property data

## Usage

``` r
extract_property_table(prop_data, table)
```

## Arguments

- prop_data:

  Property records data structure (cached format)

- table:

  Table name to extract

## Value

Single tibble for the requested table
