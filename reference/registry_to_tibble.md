# Convert Registry to Tibble

Internal function to convert the nested YAML registry structure to a
flat tibble suitable for display and filtering.

## Usage

``` r
registry_to_tibble(registry)
```

## Arguments

- registry:

  List containing the parsed YAML registry

## Value

A tibble with dataset information
